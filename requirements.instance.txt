yt-dlp
faster-whisper
# torch is expected to be preinstalled in `vastai/base-image:cuda-12.4.1-auto`.
# Dockerfile.base validates availability via `python3 -c "import torch"`.
numpy
requests
temporalio
fastapi
uvicorn
httpx
python-multipart
minio
pypinyin
